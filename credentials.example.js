// Copy this file to `credentials.js` and fill in real values.
// `credentials.js` is gitignored so secrets never leave your machine.
//
// Get these from https://developer.spotify.com/dashboard
//   1. Create an app (Web API, redirect URI = http://127.0.0.1:8080/callback)
//   2. Open Settings -> copy Client ID and View client secret
window.SPOTIFY_CREDENTIALS = {
  CLIENT_ID: "YOUR_CLIENT_ID",
  CLIENT_SECRET: "YOUR_CLIENT_SECRET"
};
